unit system;

interface

type 
  Byte = external nil name 't_byte';
  ShortInt = external nil name 't_shortint';
  Word = external nil name 't_word';
  SmallInt = external nil name 't_smallint';
  Cardinal = external nil name 't_cardinal';
  Integer = external nil name 't_integer';
  LongInt = external nil name 't_longint';
  Boolean = (False, True);
  Char = external nil name 't_char';
  QWord = external nil name 't_qword';
  
implementation

end.
